import os


class Config:
    """
    Класс централизованной конфигурации Flask-приложения.

    Что моделирует:
        Набор параметров окружения и инфраструктуры (секреты, БД, загрузка файлов).

    ООП-принципы:
        - **Инкапсуляция**: все параметры собраны в одном месте.
        - **Наследование**: можно создать DevelopmentConfig/ProductionConfig,
          переопределив только отличающиеся параметры.

    Магические методы:
        - Не переопределяются, так как класс используется как контейнер констант.
    """
    # Ключ для подписи сессий и CSRF-токенов.
    # Приоритет берётся из переменной окружения, что безопаснее для production.
    # Альтернатива: хранить в .env и загружать через python-dotenv.
    SECRET_KEY = os.environ.get('SECRET_KEY') or 'hard-to-guess-string'

    # Путь к базе данных
    # basedir нужен, чтобы путь работал независимо от текущей рабочей директории процесса.
    basedir = os.path.abspath(os.path.dirname(__file__))
    SQLALCHEMY_DATABASE_URI = 'sqlite:///' + os.path.join(basedir, 'instance', 'pharmacy.db')
    # Отключение трекинга экономит память; функция редко нужна в реальных проектах.
    SQLALCHEMY_TRACK_MODIFICATIONS = False

    # Для загрузки файлов
    UPLOAD_FOLDER = os.path.join(basedir, 'uploads')
    # Ограничение размера файла в байтах: 16 * 1024 * 1024 = 16MB.
    # Альтернатива: гибкая настройка через env-переменную.
    MAX_CONTENT_LENGTH = 16 * 1024 * 1024  # 16MB max file size